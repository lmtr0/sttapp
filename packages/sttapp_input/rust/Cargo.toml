[package]
name = "sttapp-input"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "sttapp_input"
crate-type = ["cdylib", "rlib"]

[dependencies]
enigo = { version = "0.6.1", default-features = false, features = ["wayland", "x11rb"] }
