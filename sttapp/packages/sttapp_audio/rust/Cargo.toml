[package]
name = "sttapp-audio"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "sttapp_audio"
crate-type = ["cdylib"]

[dependencies]
flacenc = { version = "0.5.1", default-features = false }
rodio = { version = "0.22", default-features = false, features = ["recording"] }
